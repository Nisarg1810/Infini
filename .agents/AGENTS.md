# Workspace Rules

- **Git Pushing**: Do NOT run `git push` automatically after making changes or commits. Only commit locally or wait for explicit user instruction/approval before pushing to the remote repository.
